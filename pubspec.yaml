name: l10n_scanner
description: Analyze Flutter l10n usage to find hardcoded text, untranslated strings, and unused localization keys.
version: 0.1.0
resolution: workspace
homepage: https://github.com/iamnabink/flutter_l10n_scanner
repository: https://github.com/iamnabink/flutter_l10n_scanner
issue_tracker: https://github.com/iamnabink/flutter_l10n_scanner/issues

environment:
  sdk: ^3.10.0

dependencies:
  yaml: ^3.1.3

dev_dependencies:
  flutter_lints: ^6.0.0

executables:
  l10n_scanner: l10n_scanner
